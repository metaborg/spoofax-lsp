package org.metaborg.spoofax.lsp.services.workspace

/**
 * A [VirtualWorkspaceService] is a virtual workspace synchronised with the connected clients' workspace.
 *
 * Here, storage is handled using the Apache Virtual File System (VFS). A Virtual Workspace relies on being
 * initialised by a root folder representing the root file of the workspace.
 *
 * @author  Thomas Kluiters
 * @since   1.0.0
 */
interface VirtualWorkspaceService {



}